# Information Structures Access Assignment

Three different information structures using three different access technologies.

## Information Structures

1. JSON accessed through an API over HTTP
2. HTML accessed through a Python web-reading library
3. CSV accessed locally after download or local file creation

## How to Run

Install required libraries:

```bash
pip install -r requirements.txt
